# OpenTitan overlay (SCORE)

| Path | Purpose |
|------|---------|
| `files/setup_score_env.sh` | Generated host env (venv, Verilator 4.210, PATH). Copied into `tools/opentitan/` by install. |

Do not commit dirty files inside `tools/opentitan`; keep SCORE copies here.
